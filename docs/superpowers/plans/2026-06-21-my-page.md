# “我的”页面 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为足球竞猜小程序新增「我的」Tab 页，展示当前用户的历史投注记录与已结算合计收益。

**Architecture:** 后端给 `Bet` 增加到 `Match` 的关联，新增 `GET /api/my/bets` 返回当前用户带比赛信息的投注列表。前端把首页改为 Tab1、「我的」为 Tab2；「我的」页 `onShow` 时按登录态决定是否请求；合计收益由纯函数 `sumSettledProfit` 在前端累加已结算净盈亏。记录行用新组件 `MyBetRecord.vue`，复用现有 `recordOddsText` / `settlement` 工具。

**Tech Stack:** uni-app (Vue 3, mp-weixin)、TypeScript、decimal.js、vitest；后端 Fastify + sequelize-typescript（Postgres）。

**对应设计文档：** [docs/superpowers/specs/2026-06-21-my-page-design.md](../specs/2026-06-21-my-page-design.md)

---

## 文件结构

**前端**
- `frontend/src/utils/format.ts` —— 修改：`formatMoney` 改为「最多两位小数」（金额格式唯一来源）。
- `frontend/src/utils/format.test.ts` —— 修改：更新 `formatMoney` 用例。
- `frontend/src/utils/bet.ts` —— 修改：`settlement`/`potentialWin` 复用 `formatMoney`；新增 `sumSettledProfit`。
- `frontend/src/utils/bet.test.ts` —— 修改：更新 `settlement`/`potentialWin` 用例；补 `sumSettledProfit` 用例。
- `frontend/src/types.d.ts` —— 修改：新增 `MyBet` 接口。
- `frontend/src/pages/my/MyBetRecord.vue` —— 新建：单条投注记录行（对阵+日期 / 金额+结算）。
- `frontend/src/pages/my/index.vue` —— 新建：「我的」页（合计收益 + 记录列表 + 登录态/空态）。
- `frontend/scripts/gen-tabbar-icons.mjs` —— 新建（一次性脚本），生成图标后删除。
- `frontend/src/static/tabbar/{home,home-active,mine,mine-active}.png` —— 新建：4 张 81×81 图标。
- `frontend/src/pages.json` —— 修改：注册新页 + 新增 `tabBar`。

**后端**
- `server/src/db/models/Bet.ts` —— 修改：新增 `@BelongsTo(Match)` 关联。
- `server/src/routes.ts` —— 修改：新增 `getMyBets` handler + 注册 `/api/my/bets`。

> 说明：Task 1 统一**金额**展示为「最多两位小数」（`formatMoney` 为唯一来源，`settlement`/`potentialWin` 复用之），**赔率/水位（`displayOdds`）不受影响、保持固定两位**。这同时修复了既有 3 个 `settlement` 红用例（旧期望 `"+95.00"`，新约定 `"+95"`），给后续 TDD 干净基线。已合规、本计划不改：详情页 admin 模拟收益（已用 `toDecimalPlaces(2)`）、`Number(bet.amount)`（整数投注额）。

---

### Task 1: 统一金额展示为「最多两位小数」（赔率不受影响）

**目标：** 金额（投注额、预计可赢、结算净盈亏、合计收益）统一为「最多两位小数」——四舍五入到 2 位后去掉末尾多余的 0；`formatMoney` 作为唯一来源，`settlement`/`potentialWin` 复用它。赔率/水位 `displayOdds` 保持固定两位小数、**不改**。

**Files:**
- Modify: `frontend/src/utils/format.ts:37-39`（`formatMoney`）
- Modify: `frontend/src/utils/bet.ts`（import、`potentialWin`、`settlement`）
- Test: `frontend/src/utils/format.test.ts:22-25`
- Test: `frontend/src/utils/bet.test.ts`（`potentialWin`、`settlement` 块）

- [ ] **Step 1: 改写 `formatMoney` 测试为「最多两位小数」**

把 `frontend/src/utils/format.test.ts` 的 `describe("formatMoney", ...)` 整块替换为：

```ts
describe("formatMoney", () => {
  it("整数无小数", () => expect(formatMoney("95")).toBe("95"));
  it("接受数字", () => expect(formatMoney(50)).toBe("50"));
  it("去掉末尾多余的 0", () => expect(formatMoney("95.50")).toBe("95.5"));
  it("保留有效两位", () => expect(formatMoney("95.55")).toBe("95.55"));
  it("超过两位四舍五入", () => expect(formatMoney("95.555")).toBe("95.56"));
  it("零显示 0", () => expect(formatMoney("0")).toBe("0"));
});
```

- [ ] **Step 2: 改写 `potentialWin` / `settlement` 测试**

把 `frontend/src/utils/bet.test.ts` 的 `describe("potentialWin", ...)` 整块替换为：

```ts
describe("potentialWin", () => {
  it("让球：本金×(欧赔-1)", () => expect(potentialWin(100, "1.95")).toBe("95"));
  it("胜平负：本金×(欧赔-1)", () => expect(potentialWin(100, "2.10")).toBe("110"));
  it("空金额为 0", () => expect(potentialWin(0, "2.10")).toBe("0"));
  it("最多两位小数", () => expect(potentialWin(150, "1.333")).toBe("49.95"));
});
```

把 `frontend/src/utils/bet.test.ts` 的 `describe("settlement", ...)` 整块替换为：

```ts
describe("settlement", () => {
  it("未结算", () =>
    expect(settlement({ result_profit: null })).toEqual({ state: "pending", text: "待结算" }));
  it("盈利（带 +）", () =>
    expect(settlement({ result_profit: "95" })).toEqual({ state: "win", text: "+95" }));
  it("亏损", () =>
    expect(settlement({ result_profit: "-50" })).toEqual({ state: "loss", text: "-50" }));
  it("持平", () =>
    expect(settlement({ result_profit: "0" })).toEqual({ state: "flat", text: "0" }));
  it("去掉末尾 0", () =>
    expect(settlement({ result_profit: "95.50" })).toEqual({ state: "win", text: "+95.5" }));
  it("超过两位四舍五入", () =>
    expect(settlement({ result_profit: "95.555" })).toEqual({ state: "win", text: "+95.56" }));
});
```

- [ ] **Step 3: 运行确认失败**

Run: `cd frontend && npx vitest run src/utils/`
Expected: FAIL，`formatMoney`/`potentialWin`/`settlement` 多个用例失败（实现仍为 `toFixed(2)` / 未封顶的 `toString()`）。

- [ ] **Step 4: 改实现**

`frontend/src/utils/format.ts` 的 `formatMoney`：

```ts
/**
 * 金额格式化：最多两位小数（四舍五入到 2 位后去掉末尾多余的 0）
 */
export function formatMoney(amount: string | number | Decimal): string {
  return new Decimal(amount).toDecimalPlaces(2).toString();
}
```

`frontend/src/utils/bet.ts` 顶部 import（已 import `handicap`，改为同时引入 `formatMoney`）：

```ts
import { formatMoney, handicap } from "./format";
```

`frontend/src/utils/bet.ts` 的 `potentialWin`：

```ts
/**
 * 预计可赢（纯盈利）：本金 × (欧赔 - 1) = 本金 × 亚赔
 */
export function potentialWin(amount: string | number, value: string): string {
  return formatMoney(new Decimal(amount || 0).mul(new Decimal(value).sub(1)));
}
```

`frontend/src/utils/bet.ts` 的 `settlement`：

```ts
export function settlement(bet: { result_profit: string | null }): {
  state: SettlementState;
  text: string;
} {
  if (bet.result_profit === null || bet.result_profit === undefined) {
    return { state: "pending", text: "待结算" };
  }
  const p = new Decimal(bet.result_profit);
  if (p.gt(0)) return { state: "win", text: "+" + formatMoney(p) };
  if (p.lt(0)) return { state: "loss", text: formatMoney(p) };
  return { state: "flat", text: formatMoney(p) };
}
```

> 注意：`displayOdds`（bet.ts）保持 `toFixed(2)` 不变——赔率/水位不受本规则约束。

- [ ] **Step 5: 运行确认全绿**

Run: `cd frontend && npx vitest run src/utils/`
Expected: PASS，无失败用例（`format` 与 `bet` 全部通过）。

- [ ] **Step 6: 提交**

```bash
git add frontend/src/utils/format.ts frontend/src/utils/format.test.ts frontend/src/utils/bet.ts frontend/src/utils/bet.test.ts
git commit -m "refactor: 金额展示统一为最多两位小数（赔率保持不变）"
```

---

### Task 2: 新增 `sumSettledProfit` 工具（TDD）

合计已结算投注的净盈亏；未结算（`result_profit == null`）不计入。

**Files:**
- Modify: `frontend/src/utils/bet.ts`（文件末尾新增导出函数）
- Test: `frontend/src/utils/bet.test.ts`（新增 import + 新 describe 块）

- [ ] **Step 1: 写失败测试**

在 `frontend/src/utils/bet.test.ts` 顶部 import 列表加入 `sumSettledProfit`：

```ts
import {
  betCondition,
  directionLabel,
  displayOdds,
  MAX_BET,
  MIN_BET,
  oddsValue,
  potentialWin,
  recordOddsText,
  settlement,
  sumSettledProfit,
  validateAmount,
} from "./bet";
```

在文件末尾追加：

```ts
describe("sumSettledProfit", () => {
  it("空列表为 0", () => expect(sumSettledProfit([])).toBe("0"));
  it("仅未结算（null）不计入，结果 0", () =>
    expect(
      sumSettledProfit([{ result_profit: null }, { result_profit: null }]),
    ).toBe("0"));
  it("混合：跳过未结算，累加已结算净盈亏", () =>
    expect(
      sumSettledProfit([
        { result_profit: "95" },
        { result_profit: null },
        { result_profit: "-50" },
        { result_profit: "0" },
      ]),
    ).toBe("45"));
  it("小数累加精确", () =>
    expect(
      sumSettledProfit([{ result_profit: "10.5" }, { result_profit: "20.25" }]),
    ).toBe("30.75"));
  it("净亏损为负", () =>
    expect(
      sumSettledProfit([{ result_profit: "10" }, { result_profit: "-30" }]),
    ).toBe("-20"));
});
```

- [ ] **Step 2: 运行确认失败**

Run: `cd frontend && npx vitest run src/utils/bet.test.ts`
Expected: FAIL，报 `sumSettledProfit is not a function` 或导入错误。

- [ ] **Step 3: 实现 `sumSettledProfit`**

在 `frontend/src/utils/bet.ts` 末尾追加（`Decimal` 已在文件顶部 import）：

```ts
/**
 * 合计已结算投注的净盈亏；未结算（result_profit == null）不计入。
 * 返回精确的十进制字符串（如 "45"、"30.75"、"-20"、"0"）。
 */
export function sumSettledProfit(
  bets: { result_profit: string | null }[],
): string {
  return bets
    .reduce(
      (acc, b) =>
        b.result_profit === null || b.result_profit === undefined
          ? acc
          : acc.add(b.result_profit),
      new Decimal(0),
    )
    .toString();
}
```

- [ ] **Step 4: 运行确认全绿**

Run: `cd frontend && npx vitest run src/utils/bet.test.ts`
Expected: PASS，无失败用例（含新增 `sumSettledProfit` 5 个用例）。

- [ ] **Step 5: 提交**

```bash
git add frontend/src/utils/bet.ts frontend/src/utils/bet.test.ts
git commit -m "feat: 新增 sumSettledProfit 合计已结算盈亏工具"
```

---

### Task 3: 后端 —— `Bet` 关联 `Match` + `GET /api/my/bets`

**Files:**
- Modify: `server/src/db/models/Bet.ts`
- Modify: `server/src/routes.ts`

- [ ] **Step 1: 给 `Bet` 模型新增 `Match` 关联**

在 `server/src/db/models/Bet.ts` 顶部，`User` 的 import 之后新增 import：

```ts
import { Match } from "./Match";
```

在 `user` 关联声明之后（约 45-46 行）新增 `match` 关联：

```ts
  /**
   * 比赛信息
   */
  @BelongsTo(() => Match, { foreignKey: "match_id", as: "match" })
  declare match?: Match;
```

（`BelongsTo` 已在该文件 import；`match_id` 为外键，目标键默认 `Match` 主键 `id`。）

- [ ] **Step 2: 在 routes 中新增 `getMyBets` handler**

在 `server/src/routes.ts` 的 `getMatchBets` 函数之后新增（`Match`、`Bet` 均已从 `./db` import）：

```ts
/**
 * 获取当前用户的投注记录（含比赛信息）
 */
async function getMyBets(req: FastifyRequest, reply: FastifyReply) {
  const openid = req.headers.token as string;

  if (!openid) {
    reply.send({
      code: 0,
      data: [],
    });
    return;
  }

  const bets = await Bet.findAll({
    where: { openid },
    include: [
      {
        model: Match,
        as: "match",
        attributes: ["id", "team1_name", "team2_name", "match_time"],
        // INNER JOIN：保证每条记录都带得到比赛，避免前端 bet.match 为 null 崩溃。
        // v_f_match 视图保留全部比赛（含已结束），正常不会丢记录。
        required: true,
      },
    ],
    order: [["id", "desc"]],
  });

  reply.send({
    code: 0,
    data: bets,
  });
}
```

- [ ] **Step 3: 注册路由**

在 `server/src/routes.ts` 的 `routes()` 内、`app.get("/api/match/bets", getMatchBets);` 之后新增：

```ts
  app.get("/api/my/bets", getMyBets);
```

- [ ] **Step 4: 类型检查通过**

Run: `cd server && npx tsc --noEmit`
Expected: 无输出、退出码 0（无类型错误）。

- [ ] **Step 5: 提交**

```bash
git add server/src/db/models/Bet.ts server/src/routes.ts
git commit -m "feat: 新增 /api/my/bets 接口与 Bet-Match 关联"
```

---

### Task 4: 前端 —— `MyBet` 类型 + `MyBetRecord.vue` 组件

**Files:**
- Modify: `frontend/src/types.d.ts`（文件末尾新增 `MyBet`）
- Create: `frontend/src/pages/my/MyBetRecord.vue`

- [ ] **Step 1: 新增 `MyBet` 类型**

在 `frontend/src/types.d.ts` 末尾追加：

```ts
/**
 * “我的”页投注记录（投注记录 + 所属比赛信息）
 */
declare interface MyBet extends BetRecord {
  match: {
    id: number;
    team1_name: string;
    team2_name: string;
    match_time: string;
  };
}
```

- [ ] **Step 2: 创建 `MyBetRecord.vue`**

新建 `frontend/src/pages/my/MyBetRecord.vue`：

```vue
<script setup lang="ts">
import { recordOddsText, settlement } from "@/utils/bet";
import { formatMatchTime } from "@/utils/format";
import { type PropType } from "vue";

defineProps({
  bet: {
    type: Object as PropType<MyBet>,
    required: true,
  },
});
</script>

<template>
  <view class="record">
    <view class="rec-left">
      <text class="rec-match"
        >{{ bet.match.team1_name }} vs {{ bet.match.team2_name }}</text
      >
      <text class="rec-odds"
        >{{ formatMatchTime(bet.match.match_time) }} ·
        {{ recordOddsText(bet, bet.match) }}</text
      >
    </view>
    <view class="rec-right">
      <text class="rec-amount">{{ Number(bet.amount) }}</text>
      <text class="rec-settle" :class="settlement(bet).state">
        {{ settlement(bet).text }}
      </text>
    </view>
  </view>
</template>

<style lang="scss" scoped>
@import "../../styles/tokens.scss";
.record {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 22rpx 26rpx;
}
.rec-left {
  min-width: 0;
  flex: 1;
}
.rec-match {
  display: block;
  font-size: 26rpx;
  color: $c-text;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.rec-odds {
  display: block;
  margin-top: 4rpx;
  font-size: 22rpx;
  color: $c-text2;
}
.rec-right {
  text-align: right;
  flex-shrink: 0;
  margin-left: 16rpx;
}
.rec-amount {
  display: block;
  font-size: 22rpx;
  color: $c-text2;
}
.rec-settle {
  display: block;
  margin-top: 4rpx;
  font-size: 28rpx;
  font-weight: 500;
}
.rec-settle.win {
  color: $c-green-bright;
}
.rec-settle.loss {
  color: $c-red;
}
.rec-settle.pending,
.rec-settle.flat {
  color: $c-text2;
  font-weight: 400;
}
</style>
```

- [ ] **Step 3: 提交**

```bash
git add frontend/src/types.d.ts frontend/src/pages/my/MyBetRecord.vue
git commit -m "feat: 新增 MyBet 类型与 MyBetRecord 组件"
```

> 说明：本任务无单测（项目未配置 Vue 组件测试），编译校验在 Task 7 的 `yarn build` 统一进行。

---

### Task 5: 前端 —— 「我的」页面 `pages/my/index.vue`

**Files:**
- Create: `frontend/src/pages/my/index.vue`

- [ ] **Step 1: 创建页面**

新建 `frontend/src/pages/my/index.vue`：

```vue
<script setup lang="ts">
import { api, getToken } from "@/api";
import { sumSettledProfit } from "@/utils/bet";
import { formatMoney } from "@/utils/format";
import { onShareAppMessage, onShow } from "@dcloudio/uni-app";
import Decimal from "decimal.js";
import { computed, ref } from "vue";
import MyBetRecord from "./MyBetRecord.vue";

const bets = ref<MyBet[]>([]);

/**
 * 每次进入刷新；未登录不请求接口
 */
const refresh = async () => {
  if (!getToken()) {
    bets.value = [];
    return;
  }
  try {
    const ret = await api<MyBet[]>({ url: "/api/my/bets" });
    if (ret.code === 0) {
      bets.value = ret.data || [];
    }
  } catch (e) {
    uni.showToast({ title: "网络异常，请重试", icon: "none" });
  }
};

/**
 * 合计收益（已结算净盈亏之和）
 */
const total = computed(() => {
  const sum = sumSettledProfit(bets.value);
  const d = new Decimal(sum);
  let state: "win" | "loss" | "flat";
  if (d.gt(0)) state = "win";
  else if (d.lt(0)) state = "loss";
  else state = "flat";
  const money = formatMoney(sum);
  return {
    state,
    text: state === "win" ? `+${money}` : money,
  };
});

/**
 * 未结算笔数
 */
const pendingCount = computed(
  () => bets.value.filter((b) => b.result_profit === null).length,
);

onShow(() => {
  refresh();
});

onShareAppMessage(() => {
  return {};
});
</script>

<template>
  <view class="mine">
    <view class="summary">
      <text class="summary-label">合计收益</text>
      <text class="summary-value" :class="total.state">{{ total.text }}</text>
      <text v-if="pendingCount > 0" class="summary-tip"
        >未结算 {{ pendingCount }} 笔不计入</text
      >
    </view>

    <view class="sec-head">
      <text class="sec-title"><text class="bar gray" />投注记录</text>
    </view>

    <view v-if="bets.length > 0" class="records">
      <MyBetRecord
        v-for="bet in bets"
        :key="bet.id"
        :bet="bet"
        class="record-item"
      />
    </view>
    <view v-else class="rec-empty">暂无投注记录</view>
  </view>
</template>

<style lang="scss" scoped>
@import "../../styles/tokens.scss";

.mine {
  padding: 24rpx;
}

.summary {
  background: $c-card;
  border: 2rpx solid $c-border;
  border-radius: 24rpx;
  padding: 48rpx 28rpx;
  text-align: center;
  margin-bottom: 28rpx;
}
.summary-label {
  display: block;
  font-size: 24rpx;
  color: $c-text2;
}
.summary-value {
  display: block;
  margin-top: 12rpx;
  font-size: 72rpx;
  font-weight: 600;
  color: $c-text;
}
.summary-value.win {
  color: $c-green-bright;
}
.summary-value.loss {
  color: $c-red;
}
.summary-value.flat {
  color: $c-text;
}
.summary-tip {
  display: block;
  margin-top: 12rpx;
  font-size: 22rpx;
  color: $c-text3;
}

.sec-head {
  display: flex;
  align-items: center;
  margin: 0 4rpx 14rpx;
}
.sec-title {
  display: flex;
  align-items: center;
  font-size: 26rpx;
  color: $c-text;
}
.bar {
  width: 6rpx;
  height: 24rpx;
  background: $c-green;
  border-radius: 4rpx;
  margin-right: 12rpx;
}
.bar.gray {
  background: $c-text2;
}

.records {
  background: $c-card;
  border: 2rpx solid $c-border;
  border-radius: 24rpx;
  overflow: hidden;
}
.record-item {
  border-bottom: 2rpx solid $c-line;
}
.record-item:last-child {
  border-bottom: none;
}

.rec-empty {
  text-align: center;
  color: $c-text2;
  font-size: 24rpx;
  padding: 80rpx 0;
}
</style>
```

- [ ] **Step 2: 提交**

```bash
git add frontend/src/pages/my/index.vue
git commit -m "feat: 新增“我的”页面（合计收益 + 投注记录）"
```

---

### Task 6: 生成 TabBar 图标（一次性脚本）

用纯 Node + 内置 `zlib` 手写最小 PNG 编码器，生成 4 张 81×81 PNG（不引入任何依赖）。生成后删除脚本，提交 PNG。

**Files:**
- Create（临时）: `frontend/scripts/gen-tabbar-icons.mjs`
- Create: `frontend/src/static/tabbar/{home,home-active,mine,mine-active}.png`

- [ ] **Step 1: 创建生成脚本**

新建 `frontend/scripts/gen-tabbar-icons.mjs`：

```js
import zlib from "node:zlib";
import { mkdirSync, writeFileSync } from "node:fs";
import { fileURLToPath } from "node:url";
import { dirname, join } from "node:path";

const W = 81;
const H = 81;
const OUT = join(dirname(fileURLToPath(import.meta.url)), "../src/static/tabbar");

// ---- 最小 PNG 编码（RGBA, 8bit） ----
const crcTable = (() => {
  const t = new Uint32Array(256);
  for (let n = 0; n < 256; n++) {
    let c = n;
    for (let k = 0; k < 8; k++) c = c & 1 ? 0xedb88320 ^ (c >>> 1) : c >>> 1;
    t[n] = c >>> 0;
  }
  return t;
})();
function crc32(buf) {
  let c = 0xffffffff;
  for (let i = 0; i < buf.length; i++)
    c = crcTable[(c ^ buf[i]) & 0xff] ^ (c >>> 8);
  return (c ^ 0xffffffff) >>> 0;
}
function chunk(type, data) {
  const len = Buffer.alloc(4);
  len.writeUInt32BE(data.length, 0);
  const typeBuf = Buffer.from(type, "ascii");
  const crc = Buffer.alloc(4);
  crc.writeUInt32BE(crc32(Buffer.concat([typeBuf, data])), 0);
  return Buffer.concat([len, typeBuf, data, crc]);
}
function encodePNG(px) {
  const sig = Buffer.from([0x89, 0x50, 0x4e, 0x47, 0x0d, 0x0a, 0x1a, 0x0a]);
  const ihdr = Buffer.alloc(13);
  ihdr.writeUInt32BE(W, 0);
  ihdr.writeUInt32BE(H, 4);
  ihdr[8] = 8; // bit depth
  ihdr[9] = 6; // color type RGBA
  const raw = Buffer.alloc((W * 4 + 1) * H);
  for (let y = 0; y < H; y++) {
    raw[y * (W * 4 + 1)] = 0; // filter byte
    px.copy(raw, y * (W * 4 + 1) + 1, y * W * 4, y * W * 4 + W * 4);
  }
  const idat = zlib.deflateSync(raw);
  return Buffer.concat([
    sig,
    chunk("IHDR", ihdr),
    chunk("IDAT", idat),
    chunk("IEND", Buffer.alloc(0)),
  ]);
}

function makeCanvas() {
  return Buffer.alloc(W * H * 4); // 全 0 = 透明
}
function set(px, x, y, [r, g, b]) {
  if (x < 0 || x >= W || y < 0 || y >= H) return;
  const i = (y * W + x) * 4;
  px[i] = r;
  px[i + 1] = g;
  px[i + 2] = b;
  px[i + 3] = 255;
}
function clearPx(px, x, y) {
  if (x < 0 || x >= W || y < 0 || y >= H) return;
  px[(y * W + x) * 4 + 3] = 0;
}

function drawHome(px, color) {
  // 屋顶三角：顶点(40,12) 底边(12,40)-(68,40)
  for (let y = 12; y <= 40; y++) {
    const t = (y - 12) / 28;
    const xl = Math.round(40 - t * 28);
    const xr = Math.round(40 + t * 28);
    for (let x = xl; x <= xr; x++) set(px, x, y, color);
  }
  // 墙体矩形
  for (let y = 40; y <= 68; y++)
    for (let x = 20; x <= 60; x++) set(px, x, y, color);
  // 门（挖空）
  for (let y = 52; y <= 68; y++)
    for (let x = 35; x <= 45; x++) clearPx(px, x, y);
}
function drawMine(px, color) {
  // 头：圆心(40,24) 半径13
  for (let y = 11; y <= 37; y++)
    for (let x = 27; x <= 53; x++)
      if ((x - 40) ** 2 + (y - 24) ** 2 <= 169) set(px, x, y, color);
  // 肩：梯形 y 46..72，半宽 9..31
  for (let y = 46; y <= 72; y++) {
    const t = (y - 46) / 26;
    const hw = Math.round(9 + t * 22);
    for (let x = 40 - hw; x <= 40 + hw; x++) set(px, x, y, color);
  }
}

const NORMAL = [138, 150, 172]; // #8a96ac
const ACTIVE = [25, 195, 125]; // #19c37d

mkdirSync(OUT, { recursive: true });

function write(name, draw, color) {
  const px = makeCanvas();
  draw(px, color);
  writeFileSync(join(OUT, name), encodePNG(px));
  console.log("wrote", name);
}

write("home.png", drawHome, NORMAL);
write("home-active.png", drawHome, ACTIVE);
write("mine.png", drawMine, NORMAL);
write("mine-active.png", drawMine, ACTIVE);
```

- [ ] **Step 2: 运行脚本生成图标**

Run: `cd frontend && node scripts/gen-tabbar-icons.mjs`
Expected: 打印 4 行 `wrote ...`。

- [ ] **Step 3: 校验产物存在且为 PNG**

Run: `cd frontend && ls -la src/static/tabbar && file src/static/tabbar/home.png`
Expected: 4 个 `.png` 文件；`file` 显示 `PNG image data, 81 x 81, 8-bit/color RGBA`。

- [ ] **Step 4: 删除一次性脚本**

```bash
rm frontend/scripts/gen-tabbar-icons.mjs
```

- [ ] **Step 5: 提交图标**

```bash
git add frontend/src/static/tabbar
git commit -m "chore: 新增 TabBar 主页/我的 图标"
```

---

### Task 7: 注册页面 + TabBar，并整体构建校验

**Files:**
- Modify: `frontend/src/pages.json`

- [ ] **Step 1: 改写 `pages.json`，注册新页并新增 tabBar**

把 `frontend/src/pages.json` 全文替换为：

```json
{
  "pages": [
    {
      "path": "pages/index/index",
      "style": {}
    },
    {
      "path": "pages/match/index",
      "style": {
        "navigationBarTitleText": "比赛详情"
      }
    },
    {
      "path": "pages/my/index",
      "style": {
        "navigationBarTitleText": "我的"
      }
    }
  ],
  "tabBar": {
    "color": "#8a96ac",
    "selectedColor": "#19c37d",
    "backgroundColor": "#141b2c",
    "borderStyle": "black",
    "list": [
      {
        "pagePath": "pages/index/index",
        "text": "主页",
        "iconPath": "static/tabbar/home.png",
        "selectedIconPath": "static/tabbar/home-active.png"
      },
      {
        "pagePath": "pages/my/index",
        "text": "我的",
        "iconPath": "static/tabbar/mine.png",
        "selectedIconPath": "static/tabbar/mine-active.png"
      }
    ]
  },
  "globalStyle": {
    "navigationBarTextStyle": "white",
    "navigationBarTitleText": "足球竞猜",
    "navigationBarBackgroundColor": "#141b2c",
    "backgroundColor": "#0e1320"
  }
}
```

- [ ] **Step 2: 构建确认编译通过**

Run: `cd frontend && yarn build`
Expected: 构建成功，输出至 `dist/build/mp-weixin`，无 TS/编译错误。

- [ ] **Step 3: 提交**

```bash
git add frontend/src/pages.json
git commit -m "feat: 启用底部 TabBar（主页 / 我的）并注册“我的”页"
```

- [ ] **Step 4: 微信开发者工具手动验证**

用微信开发者工具打开 `frontend/dist/dev/mp-weixin`（或 `yarn dev` 后导入），核对：
- 底部出现「主页」「我的」两个 Tab，选中态为绿色、图标切换正常。
- 已登录用户切到「我的」：合计收益正确（仅已结算计入），列表展示「队1 vs 队2 / 日期·投注方向 @亚赔」与金额、结算文案；存在未结算时显示「未结算 N 笔不计入」，且该笔显示「待结算」不计入合计。
- 未登录用户切到「我的」：合计显示 `0`、列表为「暂无投注记录」、Network 中**无** `/api/my/bets` 请求。
- 金额一律「最多两位小数」（如 `95`、`95.5`），赔率仍固定两位（如 `@0.95`）。
- 每次切回「我的」都会重新拉取（已登录时）。

---

## 自检

**1. Spec 覆盖**
- 首页作 Tab1、「我的」作 Tab2 → Task 7（tabBar）✓
- 合计收益大字在上、记录列表在下 → Task 5（summary + records 布局）✓
- 记录样式参照详情页、不显示投注人、改显示日期+对阵 → Task 4（MyBetRecord）✓
- 未结算不计入合计 → Task 2（sumSettledProfit 跳过 null）✓
- 未登录显示 0、无记录、不请求 → Task 5（`getToken()` 短路）+ Task 3（后端兜底）✓
- 每次进入刷新 → Task 5（`onShow`）✓
- 金额统一为最多两位小数、赔率不变 → Task 1（`formatMoney`/`settlement`/`potentialWin`，`displayOdds` 保持）✓

**2. 占位符扫描**：无 TBD/TODO，所有代码步骤含完整代码。✓

**3. 类型/命名一致性**：
- 后端关联别名 `as: "match"` ↔ 前端 `bet.match`（`MyBet.match`）一致。✓
- `sumSettledProfit` 在 Task 2 定义、Task 5 使用，签名一致。✓
- `MyBet` 在 Task 4 定义，Task 5 `ref<MyBet[]>` 与 `MyBetRecord` 的 prop 一致。✓
- `recordOddsText(bet, bet.match)` 第二参数结构（`team1_name`/`team2_name`）由 `MyBet.match` 满足。✓
